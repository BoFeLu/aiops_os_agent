apiVersion: v1
kind: Service
metadata:
  name: aiops-agent-service
  namespace: aiops
spec:
  selector:
    app: aiops-agent
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
