apiVersion: v1
kind: Service
metadata:
  name: aiops-agent
  namespace: aiops
  labels:
    app: aiops-agent
spec:
  type: ClusterIP
  selector:
    app: aiops-agent
  ports:
  - name: metrics
    port: 8080
    targetPort: 8080
    protocol: TCP
  sessionAffinity: None
